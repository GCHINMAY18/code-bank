#include<stdio.h>

int main()
{
     int i=1;    // i=0 if we want to get 0 also and here we have initialised i

     while (i<45)
     {
         printf("%d\n",i);
         i=i+1;
     }
     
    
    return 0;
}
